{
  "name": "bkunkle-react-app",
  "version": "0.1",
  "scripts": {
    "start": "npm --prefix=react-frontend start",
    "dev": "concurrently \"npm --prefix=react-frontend start\" \"npm --prefix=express-backend run dev\"",
    "postinstall": "npm --prefix=react-frontend install && npm --prefix=express-backend install"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
